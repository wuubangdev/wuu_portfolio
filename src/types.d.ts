export interface Project {
    id: number;
    title: string;
    imageUrl: string;
    description: string;
    linkGit: string;
}

export interface IService {
    id: number;
    title: string;
    imageUrl: string;
    description: string;
    linkGit: string;
}